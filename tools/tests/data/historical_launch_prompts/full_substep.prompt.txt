You are a substep agent.
Target node_key: component/spec_x@0.1.0
Target step: generate
Target substep: generate
orchestration_id: orch_test_001
agent_run_id: c
parent_agent_run_id: p
workflow_mode: dev
ir_ref: workspace/ir/component__spec_x__0.1.0/x_1
pipeline_ref: workspace/pipelines/component__spec_x__0.1.0/x_1
dependency_ref: workspace/ir/component__spec_x__0.1.0/x_1
skill_name: workflow-generate-generate
skill_ref: skills/workflow-generate-generate/SKILL.md
skill_must_read_refs: skills/workflow-generate-generate/SKILL.md,docs/AGENT_CONTRACT.md,docs/workflow/RUNNER_OUTPUT_CONTRACT.md,docs/workflow/CHECKS_MODULE_CONTRACT.md,workspace/ir/component__spec_x__0.1.0/x_1/spec.ir.yaml,spec/component/spec_x/tests.md
issue_severity: none
repair_strategy: none
repair_target_agent_run_id: none
repair_reason: none

**tmp area (reference by literal path)**: `allowed_tmp_root` is fixed at `workspace/tmp/c/` (already recorded in the same-named field of `output_manifests/c.json`). For a temporary file, write it with the `Write` tool to that literal path (`workspace/tmp/c/...`), for every extension alike; a Bash redirect to a file is not a write route in any position — as the whole command (`cat > ...`, which also matches no committed `permissions.allow` rule) or appended to a permitted command as a capture; the attempt costs a permission refusal (measured on the Claude Code backend; do not rely on another backend admitting it), so read a command's output from its result instead. `output_manifest_write_guard` judges only the path and does not reference the `$TMPDIR` env. Do not call bootstrap Bash such as `export TMPDIR=...`, `jq -er ...`, `printenv`, `bash -c '...'` (the root cause of the workflow stopping on a Claude Code session-sandbox approval request). A direct write to a canonical path (`workspace/pipelines/...`, `workspace/ir/...`, `*_meta.json` etc.) is done with the `Edit`/`Write` tool to a path registered in `allowed_file_tool_paths`; writing to a canonical path with a Bash heredoc is blocked by `forbid_unauthorized_file_write`. (The pipeline `lineage.json` is authored host-side by the conductor, not a leaf write.)

**Gate Runbook (conductor-resolved — run verbatim; do not re-derive args):** run these from the repo root (your cwd); do not `cd`. Where a run-gate command needs `<capability_token>`, fill it from your capabilities file.
- Audited re-read of a path that IS inside your allowed_read_roots (every such path is also readable directly, so this is optional); for a path OUTSIDE them this gate fails the orchestration — re-issue the read under allowed_read_roots instead: python3 tools/orchestration_runtime.py run-gate --repo-root . --orchestration-id orch_test_001 --gate orchestration_read --agent-run-id c --capability-token <capability_token> --args-json '{"read_path":"<PATH>"}'
- Required gate command(s) for this substep (exit code 0 required):
  python3 tools/validate_workspace_root.py
- A gate's result JSON is the last line of its stderr, and stderr is returned to you in the command result — read it there. Do not append a redirect to capture it: the permission layer refuses a Bash redirect to a file. `workspace/tmp/c/` already exists and is writable with the `Write` tool, so it needs no creating; the runtime also drops each gate's own summary there, one JSON file per gate name under `workspace/tmp/c/gate_results/`, so a later turn can re-read a result whose command output has scrolled out of your context. It holds the last run of that gate that COMPLETED: a run that reached the runtime and was refused removes it first, but one refused BEFORE that (a malformed command line, a command the permission layer refused) never ran at all, so CHECK its `args_json` / `exit_code` / `evaluated_at` against the run you mean. It also says nothing about whether the files it names have changed since. For the attempt you just made the command result is authoritative and the file is not. `access_logs/` is runtime-managed audit (the read hook appends a line per read decision, and `orchestration_read` writes one too) — never read or write it yourself.

**Task Card (conductor-resolved orientation — authoritative values, do not re-derive):** You are the `generate` substep of `generate` for `component/spec_x@0.1.0`.
- Deliverables — write each EXACTLY at the path below with the Edit/Write tool (these are the file-tool outputs your manifest authorizes):
  - workspace/pipelines/component__spec_x__0.1.0/x_1/source/src_1/source_meta.json
  - workspace/pipelines/component__spec_x__0.1.0/x_1/source/src_1/src/Makefile
  - workspace/pipelines/component__spec_x__0.1.0/x_1/source/src_1/src/spec_x_model.f90
  - workspace/pipelines/component__spec_x__0.1.0/x_1/source/src_1/src/spec_x_runner.f90
- Must-read inputs — read each IN FULL; this card does not authorize skipping or skimming any of them:
  - skills/workflow-generate-generate/SKILL.md
  - docs/AGENT_CONTRACT.md
  - docs/workflow/RUNNER_OUTPUT_CONTRACT.md
  - docs/workflow/CHECKS_MODULE_CONTRACT.md
  - workspace/ir/component__spec_x__0.1.0/x_1/spec.ir.yaml
  - spec/component/spec_x/tests.md
- This card only orients you. Your SKILL, your phase doc, and your manifest remain authoritative; if anything here conflicts with them, follow them and treat this card as stale.





Required requirements:
- **Do not `Read` your own launch prompt body (`launches/c.prompt.txt`).** Because the prompt is already passed as the input of the `Agent` tool, re-reading is unnecessary, and that path is blocked fail-closed by `read_manifest_read_guard`. `launches/c.prompt.txt` is the canonical artifact that stores, 1-to-1, the original text passed to the `Agent` tool for audit / replay use.
- Read only the contracted input.
- Write only the contracted artifacts.
- Observe the expected output and storage location.
- **verify-family substep (`compile.verify` / `generate.verify`): a `pass` must produce its declared meta output.** You MUST re-author the verified meta (`ir_meta.json` for compile, `source_meta.json` for generate) with the `Edit` / `Write` tool even when the inspection finds nothing to change (refresh an idempotent field such as `verify_attempts`). An inspect-only verify that writes nothing cannot terminate `pass`.
- **Read [docs/AGENT_CONTRACT.md](docs/AGENT_CONTRACT.md) immediately after launch and apply its full "Common agent contract" — it governs the remaining read/write permission guards, the direct `Write` / `Edit` artifact-write procedure, tmp-area rules, the no-`python3 -c`-write / no-cross-agent-read constraints, and the dev-mode fail rules. It is in your `read_manifest` (under `docs/`). The launch prompt header below carries your concrete `agent_run_id` / `orchestration_id` / role to substitute into the contract's generic placeholders.**
- Interpret the requirement definition and judgment rules only from `docs/`, `spec/`, and the relevant trial's artifacts in `skill_must_read_refs`. Do not extract rules from the implementation under `tools/`, verification `script`, test code, or validator code (a `Read`/`grep`/`sed`/`cat` of those is blocked by `forbid_tools_direct_read`).
- Use `workspace/orchestrations/orch_test_001/capabilities/c.json` as the canonical source for `capability_token`; immediately after launch, read that file, extract `capability_token`, and pass it to a subsequent `run-gate` (e.g. `orchestration_read`).
- If `capability_token` is not obtained or mismatched: do not start processing and stop with fail.
- Reference `output_manifests/c.json` as the canonical source, and immediately after launch confirm both the `allowed_output_paths` and `allowed_file_tool_paths` lists.
- Write every output artifact — managed JSON (`*_meta.json` / `verdict.json` / `diagnostics.json` …), source code, and `.yaml` / `.yml` / `.md` — directly with the `Edit` / `Write` tool, only to a path enumerated in the `allowed_file_tool_paths` of `output_manifests/c.json`. Do not use `guarded-apply-patch`. The MCP-owned `command_log.jsonl` is written only by the build-runtime MCP server, and the pipeline `lineage.json` is authored host-side by the conductor — neither is ever written with a file tool.
- The use of `run-gate --gate apply_patch_writes` and `apply-patch-gate` as a public path, a file write via shell redirection / `tee` / `sed -i` / an arbitrary command, and a write outside `allowed_output_paths` remain forbidden.
- Another agent's internal artifact (files under `capabilities/` / `output_manifests/` / `read_manifests/` / `access_logs/` / `agents/<other_agent_run_id>/` / `dialogs/` that do not correspond to your own `agent_run_id`) must not be read directly. A cross-agent read is blocked by `read_manifest_read_guard` (recoverable — re-issue under `allowed_read_roots`); routing it through `run-gate orchestration_read` instead records a `rule_source_violation` and fails the orchestration. Your own `gates/c/` results are not readable either — take a gate's outcome from its stderr, as docs/AGENT_CONTRACT.md describes, not by reading the file. (See docs/AGENT_CONTRACT.md for the full read/write guard set.)
- A `Compile` substep must not start unless the immediate dependency `node` satisfies `direct dependency compile readiness`.
- When updating `ir_meta.json` of `Compile`, record `attempt_count`, `verification_status`, `last_fail_reason`, `debug_mode`, and `context_isolated` as required, and when `context_isolated=false`, record `constraint_reason` as required.
- A `Generate` / `Build` / `Validate` substep must not start unless the immediate dependency `node` satisfies `direct dependency execution readiness`.
- Even if the immediate dependency `node` is incomplete, do not substitute by embedding the dependency's code into the target `node`'s `src/`.
- **MCP `command_log.jsonl` & runner program-output placement (phase-scoped — skip the clauses that do not apply to your substep):** *No LLM leaf runs a `build-runtime` MCP tool* — `run_linter` and `run_syntax_check` (`generate.gate`), `compile_project` (`build`) and `run_program` (`validate.execute`) are all deterministic conductor substeps. If a `command_log.jsonl` appears in your `allowed_output_paths` it is MCP/conductor-written at the canonical per-phase path and auto-injected by `record-launch`; never write it with a file tool (`docs/AGENT_CONTRACT.md` §"MCP command_log placement"). *If your substep authors, verifies, or judges runner program output* (`Generate.generate` / `Generate.verify` / `Validate.judge`), the `diagnostics.json` / `perf.json` / `raw/*` contract is [docs/workflow/RUNNER_OUTPUT_CONTRACT.md](docs/workflow/RUNNER_OUTPUT_CONTRACT.md) — and those substeps force-read it (it is in their `skill_must_read_refs`). Other substeps (e.g. `Compile.*`) have neither contract and can ignore this line. The exhaustive per-phase placement / `src/Makefile` auto-inject / cross-phase Make-rule / `run_id` literal form / `command_log_ref`-trust reference is [docs/workflow/MCP_COMMAND_LOG_PLACEMENT.md](docs/workflow/MCP_COMMAND_LOG_PLACEMENT.md), readable under `docs/` but **not** force-read. (The pipeline `lineage.json` is NOT a leaf output — the conductor authors it host-side.)
- With `repair_strategy=reuse`, limit it to a diff fix against the output of `repair_target_agent_run_id`.
- With `repair_strategy=restart`, regenerate from the contract input without reusing past output.
- **Slim warm-resume repair turn (`Generate.gate`/`Compile.static` finding, or a structural `Validate.execute` failure).** When a `reuse` repair warm-`--resume`s the producer session (claude; always-on, no env gate), the conductor renders a distinct **slim** body — sentinel `Warm-resume slim repair turn`, the rotated new ids/source/output paths, and the `failure_excerpt` to fix (the `Generate.gate` union report of its lint/syntax/static checkers, the `compile_static` gate report, or the `post_execute` / snapshot / quality-check violation recorded in the failed run's `trial_meta.json`) — and omits the SKILL/must-read/boilerplate the resumed leaf already holds (like the deterministic prompt, it is built directly, not from this template, and the launch validator applies a reduced marker set). Canonical: [docs/design/deterministic_followups.md](docs/design/deterministic_followups.md) "G1-slim".
- On completion, return the artifact references and status to the conductor.
- Keep your final message (the `launch_reply`) **terse and bounded**: a `status:` line, an `output_refs:` list, and at most ~8 lines of rationale — nothing more. Put all detail (diffs, full logs, per-check output) in your artifacts (`step_result.json` / `*_meta.json`), which the orchestration reads on demand via the gated read path; do **not** restate that detail in the reply. An over-budget reply is flagged by `record-agent-run` (and rejected when `ATMOFAB_ENFORCE_REPLY_BUDGET=1`).
