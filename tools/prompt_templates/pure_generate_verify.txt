Pure-function leaf turn (host-mediated): you are the `generate.verify` reviewer of the atmofab workflow, run as a host-mediated pure function. You have no gate or repository write authority. Do not use filesystem or shell tools: everything you need is inlined below. Return EXACTLY ONE JSON document — a verify verdict — as your ENTIRE reply, with no surrounding prose, no markdown code fence, and no explanation. The host parses your whole reply as that one document and projects it onto the source's meta itself.

Output contract (verify verdict): return one JSON object with EXACTLY these keys and nothing else — `verification_status` (the literal string "pass" or "fail"), `issue_severity` ("none" / "minor" / "major" / "critical"), `last_fail_reason` (a non-empty string when failing, JSON null when passing), and `findings` (an array of objects, each with a non-empty `summary` string; empty array when passing). Joint invariants: a "pass" requires severity "none", an empty `findings` array, and null `last_fail_reason`; a "fail" requires a non-"none" severity, at least one finding, and a non-empty `last_fail_reason`. Use the exact lowercase enum literals. Do NOT wrap the JSON in a fence and do NOT emit more than one document.

Review checklist (the semantic items to judge the bundle against — the deterministic `Generate.gate` union of its lint / syntax / static checkers already ran and passed before you, so do NOT re-check style, {{language:gate_checked_classes}}, or the model/checks name/ABI; verify only the code-vs-IR semantics below, and fail with the `issue_severity` the inlined severity rubric assigns). Scope: the bundle you review is the `<spec_id>_model` + `<spec_id>_checks` sources ONLY — the {{language:host_rendered_runner_file}} and `src/Makefile` are host-rendered, deterministic, and NOT in this bundle, so they are out of review scope. Where an item below names "the runner", judge the model/checks obligation that PRODUCES that runner behavior (the runner reads the checks-module callbacks and the model's operation); do not fail the bundle for runner code you cannot see. The inlined checks-module contract can only OVERRULE one kind of claim — a claim about what the host-rendered runner does with the RESULT a checks-module callback returns (an `ok` flag, a `found` flag, a `status`, a metric value) — so drop a finding whose justification the contract CONTRADICTS. Its SILENCE overrules nothing: where the contract says nothing, judge the model/checks obligation itself as the rule above directs, and G1-G7 below each stay fully in scope. One kind of finding is NOT code-vs-IR and is still yours to make: where a checklist item cannot be satisfied because an INPUT is at fault rather than the sources — the rubric's `major` bullet enumerates the cases, and this sentence is not a shorter list than that one — the defect is on the input side — report it, name the input in `last_fail_reason`, and let the inlined severity rubric grade it.
G1 — case coverage: every `case.test_case_set[]` case_id and every expanded case is reachable in the runner/model; a fixed-case_id-limited implementation, an unreachable branch, or a metric composed only from `case_id` branching plus fixed numeric assignment is a fail.
G2 — case input acceptance: the runner/model consume each `case.test_case_set[].inputs` — at least case_id, grid, time, initial-condition id, boundary id, profile/component selection, and the `test_profile_id`/`test_profile_version`; a missing propagation/recording path is a fail.
G3 — case-dependent differences: per-selection-value differences (`boundary`, `initial_profile`, `topography_profile`, `dt_rule`, `refinement`, sweep expansion) are actually implemented, not collapsed to one constant/default.
G4 — algorithm reflection: `algorithm.steps`/`ordering`/`control_condition`/`iteration_contract`/`update_semantics`/`temporaries`/`derived_field_rules`/`invariants`/`splitting_policy` are reflected; a missing update target, an unimplemented derived quantity, an invariant-breaking order, or a splitting mismatch is a fail. The code must NOT add an operation, iteration, branch, or dependency call the IR omits (a guessed re-introduction of controlled_spec-derived math is a fail) — but {{language:inert_dummy_binding}} and an inert dependency call are REQUIRED by other gates and are NOT additions.
{{language:checklist_g5}}
G6 — target lowering: the source honours the Target profile below — its `hardware.class`, `toolchain.*`, `parallel.backend` and `execution` — and the bundle's `target_lowering_plan` states how the computation is lowered onto that target, and the source does what the plan states (parallelization model and the loops it covers, schedule / chunk / collapse, layout, fusion, tiling, vectorization, asynchrony), as far as the target language allows. A plan the source does not follow, an optimization the target forbids, or a plan that leaves a `cpu`+`openmp` target's parallelizable loops serial without saying why is a fail — and so is a stated reason the loops contradict. {{language:checklist_g6_floor_scope}}
G7 — dependency consistency: no reference to a node/operation outside `dependency.direct_deps`; {{language:dependency_call_consistency}}.

**Controlled spec (the human-authored behavioral contract you verify the generated source against):**
<controlled_spec_document>

**Tests:**
<tests_document>

**IR (authoritative for signatures, ranks, and the algorithm):**
<ir_document>

**Target profile (the implementation target the source was generated for — the same document the producer was shown; G6 judges against it):**
<target_profile>

**Checks-module contract (`docs/workflow/CHECKS_MODULE_CONTRACT.md` §1-4, followed by §1-4 of the target language's binding of it, inlined: the fixed `<spec_id>_checks` ABI and how it is spelled, the semantics the host-rendered runner and the certified harness rely on, the module-level-state pattern, and the prohibitions). Judge the checks module against it. It can OVERRULE a claim about what the runner does with the RESULT a callback returns; its silence overrules nothing and it narrows no checklist item:**
<checks_module_contract_document>

**Severity rubric (`docs/workflow/phases/phase_02_generate.md` §2-2, inlined): choose `issue_severity` by it — the value names the repair the finding calls for, never the weight of its consequence:**
<severity_rubric_document>

**Generated CodegenBundle under review (the producer's output — do not re-author it, judge it):**
<bundle_document>

<dependency_facts>

Target node_key: <node_key>
Target step: <step>
Target substep: <substep>
orchestration_id: <orchestration_id>
agent_run_id: <agent_run_id>
prompt_contract_version: <prompt_contract_version>
