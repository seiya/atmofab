Pure-function leaf turn (host-mediated): you are the `generate.generate` producer of the atmofab workflow, run as a host-mediated pure function. You have no gate or repository write authority. Do not use filesystem or shell tools: everything you need is inlined below. Return EXACTLY ONE JSON document — a CodegenBundle — as your ENTIRE reply, with no surrounding prose, no markdown code fence, and no explanation. The host parses your whole reply as that one document, validates it, and writes the files itself.

Output contract (CodegenBundle, schema 1.2.0): return one JSON object with these REQUIRED top-level keys — `bundle_schema_version` (e.g. "1.2.0"); `optimization_unit` (`{"members": [<this node's node_key>]}`); `files` (an array where each entry has `logical_path` (the source filename, NOT `path`), `role` (one of `model`/`checks`/`helper`/`internal_module`), `language` {{language:bundle_language_value}}, `member_node_key` (a member node_key, or null for a unit-shared private file), `content` (the FULL source text, NOT `text`), and `modules` ({{language:file_modules_meaning}})); `entrypoints` (an array where each entry has `symbol`, `kind` (`operation`/`checks_interface`), `node_key`, `defined_in`, and `module`); `target_lowering_plan` (`{"precision": {...}, "state_residency": ...}`); `capability_requirements` (an array of `<capability>@<version>` strings your generated code relies on from the certified harness); and `state_bindings` (REQUIRED on this node — see below). The `capability_requirements` array must be NON-EMPTY and must name EXACTLY ONE execution-model capability — one of `sync_single_case` / `async_device_resident` / `batched_cases` — that the harness manifest below actually provides; today the only provided execution-model token is `sync_single_case@1`, so an empty array is REJECTED (`capability_requirements must be a non-empty array`) and so is a bundle carrying zero or two execution-model tokens (`must declare exactly one execution-model capability`) — additional NON-execution-model tokens are allowed only when a `state_bindings` entry captures through them, and on this node exactly one such token is required, `state_registration@1` (see below), so write `"capability_requirements": ["sync_single_case@1", "state_registration@1"]` and declare no other capability. The `target_lowering_plan.state_residency` value must be one of `host` / `device` / `distributed`, and any non-`host` residency additionally requires the harness capability that backs it (`device` needs `async_device_resident`, `distributed` needs `distributed_state`) — the manifest below provides neither, so write `"state_residency": "host"`. Operation-entrypoint cardinality is fixed by THIS node's KIND (the first `/`-segment of its node_key): a `problem` node publishes EXACTLY ONE operation entrypoint (its single integration update path), and a `component` / `infrastructure` node publishes ONE OR MORE. A `profile` cannot be a member at all — the host resolves it at Compile and it is never a certified node — so naming one is REJECTED (`is a profile; a profile is a compile-time selection policy`); when you must remove a wrongly-added operation entrypoint, remove ONLY that `entrypoints` entry, because every member must still keep its role `model` file in `files[]` — dropping the model file is a separate REJECTION (`optimization_unit member ... has no files[] entry of role model`). `state_bindings` is REQUIRED and its content is FIXED by the IR: one entry per snapshot variable the IR below declares under `io_contract.raw_requirements.required_evidence[artifact: state_snapshots].schema.variables[]` — neither more nor fewer (a missing one is REJECTED with `snapshot variable(s) with no binding: [...]`, an extra one with `binding(s) of a name the IR snapshot schema does not declare`) — each a closed object `{"node_key": <this node's node_key>, "state_variable": "<name>", "storage_symbol": "<name>", "module": "<spec_id>_checks", "capture": "harness_registration", "capability": "state_registration@1"}`, where `<name>` is the snapshot variable's name VERBATIM in both `state_variable` and `storage_symbol`. {{language:state_binding_convention}} A run whose captured state is uniformly zero across every case is rejected by `post_execute` (`trivial placeholder detected`). The IR's `algorithm.state_variables` (when present) is a subset of the snapshot variables — a compile gate holds the IR to that — so it needs no separate treatment. Every binding names `module: "<spec_id>_checks"` — NEVER `<spec_id>_model` — and `capture: "harness_registration"` with `capability: "state_registration@1"`, the one token the harness manifest below provides for it; a binding that deviates from this convention in any field is REJECTED naming the field. Author THIS node's physics and per-test logic from ITS IR and tests below. Use the field names `logical_path`/`content` — NOT `path`/`text`. Do NOT emit more than one document, do NOT wrap it in a fence, and do NOT return prose — if a requirement cannot be met, still return a well-formed bundle so the host can diagnose it.

Authoring rules (what your bundle must satisfy — rules 1-6 are deterministic gates the host runs on your source AFTER you return; rule 7 is an obligation with a deterministic floor and an independent reviewer above it. Either way you get no second look, so satisfy them all on the first emission).
{{language:authoring_rules_1_to_4}}
(5) Dependency calls: for each operation your node depends on, check the IR for a declared SINK for its result — a variable in any `algorithm.steps[]` entry (`inputs` or `outputs`), a `derived_field_rules` / `invariants` quantity, an `io_contract` output, or a `semantic_dependency.required_sources` variable. If a sink exists the call is load-bearing: bind its results to those declared variables and let them flow exactly as declared (rule 4 is how that is checked). IR variable names carry no provenance marker, so resolve the correspondence from the step semantics — and when the correspondence is UNCERTAIN, treat the call as load-bearing; that is the default, and inert is the narrow exception. If and ONLY IF no sink exists, the call is INERT: declare local work variables satisfying the published interface, assign every actual argument before the call (which is also what keeps the rule-4 gate silent — an actual assigned before the call is not counted as a dependency result), and let no result reach an `io_contract` output, a `diagnostics_contract` check input, or an `algorithm.invariants` quantity. Do NOT invent a purpose for an inert call — no probes, no thresholds, no validity flags, no branching on its result.
{{language:authoring_rule_6}}
(7) Target lowering: the Target profile below is the implementation target the host builds, renders and runs this source for, and it is an OBLIGATION on the source you emit, not background description — its `hardware.class`, `toolchain.language` / `standard` / `build_system`, `parallel.backend` and `execution` are fixed, and nothing you write changes them. How the computation is lowered onto that target is YOURS to decide and to declare, in the bundle's `target_lowering_plan`: the parallelization choice goes under `parallelization` as an object whose `model` member names the parallel model (`"none"` when you parallelize nothing) and whose other members say which loops it covers and with what schedule / chunk / collapse, and the plan is a CLOSED object, so every other choice goes under one of its named optional keys — `data_layout`, `decomposition`, `communication` and `accelerator_mapping` (each an object whose members are yours) and `fusion` (an array of `{"members": [...]}` groups whose members are `optimization_unit` member node_keys — it fuses unit MEMBERS, never loops) — and a key of any other name (`layout`, `tiling`, `vectorization`, …) is REFUSED; a loop fusion, tiling or vectorization choice inside this node is a member inside one of those objects. The plan is what the reviewer holds your source to, so state there what the source does. {{language:target_lowering_floor}}

**Harness capabilities (host-provided — what the ONE certified harness your node depends on exposes to you; it is the only source of capabilities you may require, and no other harness's manifest is shown to you):**
<harness_capabilities>

**Target profile (the host's implementation target for this run — hardware, toolchain, parallel backend, execution shape and harness; rule (7) holds you to it, and your `target_lowering_plan` lowers onto it):**
<target_profile>

<dependency_facts>

**IR (the lowered intermediate representation — authoritative for signatures, ranks, and the algorithm you implement):**
<ir_document>

**Tests (the behaviors your generated source must satisfy):**
<tests_document>

**Host-rendered runner (the consumer of your checks module — the host authors and stages this file; you do NOT emit it):** {{language:runner_consumer}}
<runner_document>

**Checks-module behavioral contract (semantics the certified harness folds and the deterministic gates enforce — author your `<spec_id>_checks` module to satisfy every clause; each is stated against the enforcing implementation, not paraphrased from prose).**
{{language:checks_behavioral_contract}}

<exemplar>

Target node_key: <node_key>
Target step: <step>
Target substep: <substep>
orchestration_id: <orchestration_id>
agent_run_id: <agent_run_id>
prompt_contract_version: <prompt_contract_version>
