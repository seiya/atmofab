{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Bash|bash|Shell|shell|apply_patch|ApplyPatch)$",
        "hooks": [
          {
            "type": "command",
            "command": "sh -lc 'ROOT=$(git rev-parse --show-toplevel) || exit 2; PYTHONPATH=\"$ROOT${PYTHONPATH:+:$PYTHONPATH}\" python3 -m tools.hooks.dev_cli --backend codex --event PreToolUse'"
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "matcher": "^(Bash|bash|Shell|shell|apply_patch|ApplyPatch)$",
        "hooks": [
          {
            "type": "command",
            "command": "sh -lc 'ROOT=$(git rev-parse --show-toplevel) || exit 2; PYTHONPATH=\"$ROOT${PYTHONPATH:+:$PYTHONPATH}\" python3 -m tools.hooks.dev_cli --backend codex --event PermissionRequest'"
          }
        ]
      }
    ]
  }
}
