# TODO
Open work across the repository, with the evidence that it is open (`docs/DEVELOPMENT.md`
§Record placement). Nothing else lives here.

**How an item is written.** One short paragraph: the title, the date it was filed, its severity,
the issue it belongs to, what is open, and the measured evidence that it is open. A measurement
episode, a review history, a rejected alternative, or a disclosure goes to a comment on that
issue, and the item links to the comment.

**How a finished item is recorded.** The pull request that finishes it removes it from this file.
That pull request's description, or a comment on the issue the item belongs to, carries the
record: what closed it, the evidence, and the measured figures with the commit they were taken
at. The commit message names the item's title and the issue or pull request number, so
`git log -S '<title>' -- TODO.md` finds it. An item is also removed when it turns out never to
have been a task. **An item with no issue** records its evidence in the pull request that finishes
it and nowhere else; if it is large enough to need a comment before then, file the issue at that
point rather than growing the item here.

## TODO list

- **Simplification program: 13 open issues under #167** (2026-09-06, canonical record `docs/design/simplification_program.md`). Open: #168 #169 #170 #171 (pure-leaf migration, in that order), #175, #176, #177, #178, #179, #180, #181, #182, #183. Premise decisions in force: no vendor lock-in, on the LLM side (#172 rejected) and on the target-stack side (#173 rejected: CUDA, C++ and Python targets are planned) (`AGENTS.md` §Development premises); `Tune` / `Promote` are required future flows whose reserved surface stays (#174 rejected); the `--mode prod` / `escalate` diagnostician stays and goes pure (#169); `profile` is a host-resolved compile-time selection policy, and runtime scheme selection belongs to the future `spec`-driven model driver `node` (#175). Each issue states its completion criterion; this item closes when the last of them does.

- **A dependency regeneration silently breaks a certified consumer: readiness cannot see it (B), and a `component`'s published ABI is not pinned (A)** (2026-09-04, high, issue #153). **(A) is landed, and what stays open is what the two billed closures did not exercise**: B's CONTENT arm has no billed instance (no `model_source_sha256` was ever compared — every node was rejected on the missing key first), the §5.1-to-certified-source agreement is measured once and is not a standing test, and two CARRIED residues are unfixed — an interface-only published operation that both surface gates pass, and five ABI-identical spellings the signature comparison refuses. Evidence: at `2dd73cd` all seven surface-publishing specs' §5.1 agree with their certified source except `boundary1d`, which disagrees on order and on three of its five argument names; the row that checked it read the gitignored `workspace/` and was removed in `f81c83f`, so a later spec edit that moves a §5.1 away from its source reddens nothing. Record: https://github.com/seiya/atmofab/issues/153#issuecomment-5559622300 .

- **`_RUBRIC_POINTER_SURFACES` is a hand-curated literal, and at least one document states the `issue_severity` routing with no pointer at either rubric** (2026-09-03, low, issue #148). Two halves are open: sweep `docs/` for statements of the `issue_severity` routing that carry no pointer to a rubric, and replace the hand-curated `_RUBRIC_POINTER_SURFACES` with a derivation. It matters because a leaf's manifest grants `docs/` unconditionally, so a leaf can meet the routing without the rule for choosing the value. Evidence: measured at `72f39c4`, `docs/design/deterministic_followups.md` states the full routing and names `classify_verify_severity` with no pointer at either `#### Severity of a finding`; `_RUBRIC_SECTION_BY_STEP`'s `§1-2` / `§2-2` are literals coupled to nothing, so renumbering leaves all six pointer lines naming a section that no longer exists, green. Record: https://github.com/seiya/atmofab/issues/148#issuecomment-5560464518 .

- **Three `fail`s `skills/workflow-compile-verify/SKILL.md` mandates outside V1–V8 have no rubric bullet to take a value from** (2026-09-03, low, issue #148). Open as a QUESTION, not an edit: should the `SKILL` still mandate a `fail` for a shape its own host cannot produce? Issue #148 tried grading the three in the `major` bullet and reverted — `record-launch` and `allowed_output_paths` make two of them unreachable, so naming them gave a leaf a rubric-sanctioned exit from the substep with no V1-V8 check performed. Evidence: measured at `72f39c4`; the meta-key case is routed as `compile_fail_meta_schema` before severity is read, so no route was established for any of the three. Record: https://github.com/seiya/atmofab/issues/148#issuecomment-5560464842 .

- **`issue_severity` is not a required `ir_meta.json` / `source_meta.json` key, and both rubrics now tell the leaf to write it** (2026-09-03, low, issue #148). The fix is one clause in the required-keys section (present WHEN `verification_status=fail`), not a new key in the type contract. Evidence: measured at `c01ddbf` — `docs/workflow/phases/phase_01_compile.md` and `tools/meta_contracts.py` both list five keys and `issue_severity` is in neither, while §1-2's rubric and `skills/workflow-compile-verify/SKILL.md` both tell the leaf to write it; a leaf that reads the required-keys section as closed omits it and the conductor routes `compile_fail_unclassified` instead of the value the leaf chose. Record: https://github.com/seiya/atmofab/issues/148#issuecomment-5560465144 .

- **Both severity rubrics justify their second tie-break with "a producer re-run either way", which is not what `dev` does for `critical`** (2026-09-03, low, issue #148). Open because the fix is a coupled edit: the justifying clause is byte-identical in `phase_02_generate.md` §2-2 and `phase_01_compile.md` §1-2, so changing one desynchronizes the pair, and `docs/RUNBOOK.md` §3-1's `critical` remedy has to be re-read with them. Evidence: measured at `c01ddbf` — `classify_verify_severity` routes `critical` in `dev` to `fail_closed`, and it is the operator's cold `--resume` that is the producer re-run, which makes the sentence defensible rather than true as written.

- **`Tune.verify` has no `issue_severity` rubric** (2026-09-03, low, issue #143). Extending the rubric requires stating first which repair a `Tune.verify` `minor` calls for, because the conductor does not drive `Tune`. Evidence: measured at `7144404` — `skills/workflow-tune-verify/SKILL.md:22` states only the `dev` routing consequence of `major|critical` and assigns no value; `tools/workflow_conductor.py` names only `compile` and `generate` in its same-phase repair branch, so there is no `minor` route to name.

- **The diagnostician's severity rubric is a twin with no coupling test** (2026-09-03, low, issue #143). TWO questions, and only the first is cheap: whether the twin needs a coupling test, and whether the two personas should grade on one axis at all. Evidence: measured at `0caaf52` — `skills/workflow-escalate/SKILL.md` grades "how compromised the existing artifacts are" while `_DIRECTIVE_SCHEMA` grades "how disruptive the defect is", the weight-of-consequence axis `phase_02_generate.md` §2-2 explicitly refuses, and their `major` rows point opposite ways; `test_diagnosis_prompt_renders_escalate_skill` pins a substring only, so a rewritten SKILL rubric that disagrees with the schema ships green. Record: https://github.com/seiya/atmofab/issues/143#issuecomment-5560393828 .

- **`audit_orchestration`'s report-wide loaders have no "must not swallow" pin, unlike the dangling-launch path** (2026-09-02, low, issue #130). The fix is one pin of the same shape as `CollectorsDoNotSwallowTests` — inject at `Path.read_text` and require it to reach `audit()`'s caller. NOT a hole today: the shipped code fails loud. Evidence: measured at issue #130's round-5 HEAD — wrapping `_load_jsonl_with_errors`'s `read_text` in `try/except Exception: return [], []` leaves both test files green (164 passed) while producing a completely clean report, all counts 0, exit 0. What it records is that #130's invariant is pinned on the dangling-launch path only, while the report's own numbers are the wider surface. Record: https://github.com/seiya/atmofab/issues/130#issuecomment-5560465478 .

- **`skills/workflow-audit-claude/SKILL.md` Step 2 enumerates AGENTIC leaves only, and says nothing about it** (2026-09-02, low, issue #137). What closes it is measuring Step 2 against all three pure leaf shapes and the transcript retention window, which nobody has done — the branch that measured the defect wrote a recovery note and had it measured false three ways in the next round, and reverted the whole edit. Evidence: over `workspace/orchestrations/` with `os.walk`, 23 orchestrations hold pure leaves, 64 pure leaves in total, and 0 of their `agent_run_id`s appear in ANY `native_hook_events.jsonl`, while 91 of 239 agentic launches do; two reviewers and the author re-derived 23/64/0 independently. Record: https://github.com/seiya/atmofab/issues/137#issuecomment-5560394119 .

- **`skills/workflow-audit-claude/SKILL.md` still carries a pre-conductor premise about session ids** (2026-09-02, low, issue #137). What closes it is driving Step 2's `agent_role` determination on a real conductor-era orchestration and recording what the session ids actually are, which no one has done — the previous attempt to edit this document on adjacent evidence was measured FALSE in the next round and reverted. Evidence: line 490 says the orchestration agent and a child agent are mixed under one `session_id`; under `tools/workflow_conductor.py` each leaf is pinned to its own `--session-id <agent_run_id>` and the conductor is a plain Python process with no session, so the mixing cannot occur for a conductor-driven run. Record: https://github.com/seiya/atmofab/issues/137#issuecomment-5560465785 .

- **A leaf shortcut cannot be reached through the audit, but the swallow class is not closed as a general rule** (2026-09-02, low, issue #130 — DECLARED SCOPE rather than a task). A strong claim would need the no-swallow rule stated once over the whole module — the function set derived from the call graph under `build_launch_incident` and the primitive set from what those functions call — instead of the two hand-maintained lists. Evidence: measured at that branch's tip, three instruments hold it (primitive injection over `Path.read_text` / `is_dir` / `is_file` / `glob`, `_blanket_swallows` over three named functions, a typed sweep over the detector call), and all three are pinned to the functions and primitives named, so a swallow anywhere else is outside all of them. Record: https://github.com/seiya/atmofab/issues/130#issuecomment-5560394424 .

- **`_load_json_if_dict` degrades a corrupt `orchestration_meta.json` to `{}` in silence** (2026-09-02, low, issue #130). **Not reachable in this file and kept for the SHAPE**: fix it when a non-atomic writer of some other `_load_json_if_dict` input makes it reachable. The swallow reaches neither integrity channel — `data_integrity_warning` counts jsonl parse errors only and `diagnostic_failures` records only what the three best-effort handlers catch. Evidence: measured at `ff3d7a9`, all 15 writers touching `meta_path` in `tools/orchestration_runtime.py` go through `_atomic_write_text` (`mkstemp` + `os.replace`), zero non-atomic, so a corrupt `orchestration_meta.json` needs storage corruption or a hand edit — a path only the operator can reach. Record: https://github.com/seiya/atmofab/issues/130#issuecomment-5560394734 .

- **`preset=mixed` refuses its own lint gate on a node whose tree holds nothing one sub-preset can
  analyse** (2026-09-01, low, issue #120). The fix belongs to the COMPOSITE, not to a backend: a sub-preset should be skipped over a tree holding nothing it can analyse, which is a change to `tool_run_linter`'s composite branch or to `_gate_lint_check`. Unreachable today — `_validate_toolchain_backend_supported` refuses `language: mixed` on every non-`infrastructure` node and the corpus is `fortran` throughout. Evidence: measured on Cppcheck 2.7 and 2.17.1, over a directory holding only `main.f90`, `cppcheck` exits 1 with `could not find or open any of the paths given`, classified as a REFUSAL, so `_raise_on_unusable_lint_invocation` raises a transport `fail_closed`. Recorded in `docs/backends/linter/cppcheck/RULES.md` §Limits. Record: https://github.com/seiya/atmofab/issues/120#issuecomment-5560395056 .

- **Two ways a `Generate.gate` lint step reports exit 0 over source it did not read, on the
  linter that is actually reachable** (2026-09-01, low, issue #120). ONE caller-side check closes both: refuse a lint run reporting zero files scanned over a directory the conductor knows is non-empty — a change to `_gate_lint_check`, existing machinery #120 does not touch. The unreadable-directory half buys a leaf nothing (a source hidden from the linter is hidden from the compiler, and `_write_makefile` never globs); the SYMLINKED-directory half is the stronger one, because a symlinked directory COMPILES, so what bounds it is the leaf's write authority. Evidence: measured on this host — `chmod 000` on a subdirectory gives `fortitude` `0 files scanned, 1 could not be read`, exit 0, and `ruff` the same; a symlinked directory whose target is outside the walk root gives `0 files scanned`, exit 0, on all four ruff builds and on fortitude; `cppcheck` fails closed in both. Pinned as measured non-closures by `test_an_unreadable_directory_is_a_measured_NON_closure` and `test_a_symlinked_directory_outside_the_root_is_a_measured_NON_closure`. Record: https://github.com/seiya/atmofab/issues/120#issuecomment-5560395355 .

- **What issue #102 left unexercised and un-gated** (2026-08-26 — disclosure, not a plan, issue #102). Three things the change was never exercised against, all still true: no orchestration has ever been run against it (twelve commits, five review rounds, 5278 tests, zero leaves launched, on the entrypoint every leaf tool call goes through); no preflight executes the leaf hook chain any more, so an import-time defect in `tools/hooks/cli.py` would switch every policy off for a whole run with nothing gating it; and merging re-prompts once for codex hook trust, which is expected. Evidence: `leaf_config/claude/settings.json` is byte-unchanged, the prepared leaf homes are sha256-identical to `origin/main`'s, and `preflight` returns the same verdict for every check on both backends against both revisions — which is what lowers the risk and is not the modality carrying it. Record: https://github.com/seiya/atmofab/issues/102#issuecomment-5559623885 .

- **The suite no longer answers a question about the machine it runs on** (2026-08-26 to 08-27, issue #84 — closed with residuals). **Five residuals, and one thing still open at the cap.** The residuals: `env -i` is not covered (`PATH`, `HOME` and the locale family are still inherited); the guard reasons about `ATMOFAB_*` and not about the rest of the ambient environment; the CPU-budget calibrator bounds load but not an arbitrarily slow host; the depth and breadth witnesses cover the routes measured, not every route; and the whole mechanism lives in test files, so hunk mutation does not apply to it. Still open: `-p no:terminal` gives a SILENT strip — 0 disclosures — and no documented invocation uses it, which is why the header producer alone is not enough. Evidence: FINAL STATE at `1212e62`, primary checkout, suite **5304 passed, 0 skipped** clean and under a poisoning of every `ATMOFAB_*` name, and identical in a `/tmp/wt84` worktree and a `/tmp/a/b/c/deep` one — the path-depth coupling is gone. The loop stopped at its budget (round 0 plus five, the cap), not at convergence. Record: https://github.com/seiya/atmofab/issues/84#issuecomment-5560395673 .

- **Two rough edges the issue-#102 review named and did not fix** (2026-08-26, issue #102 — review round 1, measured). Two are unpinned-but-correct and one is an operator-facing shape worth recognising: the isolated codex home's SHA-256 re-verification cannot fail (it compares bytes it just wrote from the same in-process buffer — neutering it to `if False:` survives the suite, 2554 passed), `.codex/hooks.json` has no shape check any more (deliberate: it governs the operator's own session, which `AGENTS.md` §Development premises puts out of scope), and a claude leaf that has lost `ATMOFAB_ORCHESTRATION_ID` gets rc 2 on `Stop` as well, blocking the stop rather than ending the leaf — the fail-closed direction, ended by the conductor's own timeout. Record: https://github.com/seiya/atmofab/issues/102#issuecomment-5560395980 .

- **The isolated backend homes are durable, and their retention is manual** (2026-08-20, issue #64 — measured). One follow-up is deliberately not done and one failure mode is stated rather than hidden. Not done: a `workflow_homes_root_creatable` preflight probe, which would surface an unusable `~/.atmofab` at launch instead of at the first leaf; skipped because it ripples through roughly 30 launchable-preflight fixtures for a case the first launch already fails closed on, by name and with the path in the message. Stated: the leaf directory is created EXCLUSIVELY, so a crash between that `mkdir` and the metadata write leaves an unrecorded directory that refuses the next launch — the fail-closed direction, whose remedy is NOT one command (a terminalize then a plain `--delete` for the crash case, `--allow-unverifiable --delete` only for a run whose metadata is gone). Retention stays manual: `tools/prune_workflow_homes.py` is the only thing that removes a home and `test_nothing_is_wired_to_invoke_this_tool_automatically` samples for a caller appearing later. Record: https://github.com/seiya/atmofab/issues/64#issuecomment-5560396289 .

- **The leaf's configuration surface AND its environment surface are closed** (2026-08-19; environment face 2026-08-20, medium, issue #63 — measured). **Still open, named rather than fixed**: four environment sources the allowlist does not reach — the deterministic substep's `merged_env = os.environ.copy()` (`mcp_servers/build_runtime_server.py`, reached in-process via `_run_deterministic_substep`); the `sh -lc` login shell inside the sandbox sourcing the operator profile; `run_workflow.py`'s own `base_env = dict(os.environ)`, filtered on the way to a leaf but not at its source; and the conductor's host-side `env=self.env` subprocesses, which are the host's own tools rather than leaves. Evidence: measured on this host, only `PATH` comes back from the login shell, rewritten by `/etc/profile` + `~/.profile`, so a hook's PATH is the profile's rather than the allowlisted one. Record: https://github.com/seiya/atmofab/issues/63#issuecomment-5559624485 .

- **Migrate the backend knowledge that is still inlined in the neutral core** (2026-08-14, high, issue #182 — measured, structural). The rule is written and mechanically bounded (`docs/BACKEND_BOUNDARY.md`, `tools/backends/registry.py`, `tools/tests/test_backend_boundary.py`); what remains is paying the debt, area by area. Record: https://github.com/seiya/atmofab/issues/182#issuecomment-5559624872 .
  - **The token ratchet is FROZEN (issue #182, 2026-09-06): the suite no longer compares the tree against `tools/tests/data/backend_boundary_baseline.json`; `python3 -m tools.tests.test_backend_boundary --check-baseline` does, on request.** Reason, measured at `c131639`: the baseline was rewritten in 37 commits over the whole of its life, 2026-08-14 to 2026-09-06. Of the **70** pull requests merged in that window — the honest denominator, since the file did not exist before it; an earlier version of this bullet said 4 of the 105 merged since 2026-07-08, which is true and dilutes the rate with 35 pull requests that could not have touched a file that did not exist — **14 touched the baseline at all (20%)** and **4 (#115, #118, #154, #164) touched it and no other backend-boundary artifact (5.7%)** — that is the criterion, and each of those four changed 10-19 files overall, so "touched only that file" would be false read literally — on work whose subject was not the boundary; until a second target exists the sample has no second consumer. The ledger continues. What still runs in the suite: the direct-import pin, the reach pins (`RootFileCoverageTests`, `ScopePinTests`, `ScannedSetTests`, `TokenClassReachTests`), the registry tests, and the synthetic witnesses of the comparison (`BaselineComparisonTests`). Drift while frozen is expected: unrelated pull requests move counts and nothing regenerates, so a ledger pull request runs `--check-baseline` on `origin/main` first, then on its branch, attributes the difference, judges each finding by `docs/BACKEND_BOUNDARY.md` §Decision Criteria, regenerates with `--write-baseline` in the same pull request, and records both outputs there — that regeneration is the measurement this ledger exists for.
  - **The freeze also stops the suite constraining the baseline FILE, and that direction is not obvious from the sentence above**: a committed baseline with a ceiling raised to any number is green on the default suite at HEAD, where `origin/main` went red — measured with a `{"docs/GARBAGE.md": {"fortran": 9999}}` baseline, 2 failed / 89 passed at `05658bf` against 110 passed on the freeze branch — and while frozen nothing regenerates, so an inflated ceiling survives to the unfreeze. The recorded figures are the only cross-check: baseline at the freeze **53 files / 2,440 sampled occurrences**, unchanged from `05658bf`, and the removal procedure's step (2) starts by comparing against them. CI does not close this either: `.github/workflows/tests.yml` runs `pytest tools/tests/` and has no `--check-baseline` step, so the ratchet is in no automated run at all while frozen. **OPEN OBSERVATION, due 2026-10-06** (30 days after PR #187 merged as `4eff3f3` on 2026-09-06): completion criterion 2 of issue #182 is that no pull request other than a ledger one touches the baseline while frozen, and it can only be checked afterwards. Run `git log --oneline 4eff3f3.. -- tools/tests/data/backend_boundary_baseline.json`; every commit it lists should belong to a pull request that migrated an area of this ledger. The last commit before the freeze is `97a085a`. If it lists anything else, the freeze is not holding: record which pull request, and why the reader regenerated, HERE before deciding what to change — the issue is closed, so this bullet is the only place the observation is owed to. Delete this sentence once the observation is taken and its result is written down. **REMOVAL CONDITION**: the first pull request of the second `(language, hardware)` target.
  - **REMOVAL PROCEDURE**, in that pull request: (1) re-add a default-running `TokenRatchetTests` asserting `grown_entries(...)` and `stale_entries(...)` are empty on the real tree — the shape of `TokenRatchetTests` in `git show 05658bf:tools/tests/test_backend_boundary.py` (find it by name: three reviewers read three different line ranges off it), written over the two module functions; keep `BaselineComparisonTests`; (2) run `--write-baseline` once and state the growth since the freeze in that pull request; (3) unwrite every statement of the freeze. **They all carry the marker `#182`, and nothing permanent does** — that is the rule this branch converged on after the hand enumeration here went stale three times in three rounds, each time because a commit added a statement and did not come back to this list. So: `git grep -n '#182'` outside `docs/design/` (historical decision records, which stay), read every hit, and unwrite it. What must SURVIVE carries no marker and is stated apart from the freeze prose on purpose: the permanent-commands paragraph of `docs/BACKEND_BOUNDARY.md` §Enforcement (the exit-2 refusals and `--write-baseline`'s preview) and `_print_what_is_about_to_be_blessed`'s docstring, which says so in its first line. Adding a freeze statement without the marker, or marking a permanent one, is the way this step goes wrong; check both directions against the grep before calling step (3) done. And delete this bullet.
  - Area ledger. Each area's acceptance is the same: its knowledge lives under `tools/backends/<axis>/<backend_id>/`, the neutral core reaches it through the registry, and its allowlist entry disappears. **No per-area token count is written here.** The counts this ledger used to carry went stale with every commit and one of them was quoted for three rounds after it was wrong; re-take them with `python3 -m tools.tests.test_backend_boundary --check-baseline` against `tools/tests/data/backend_boundary_baseline.json`, which is the only reader whose spelling and file set are defined.
    - **OPEN — gates that still spell their own implemented set.** `_validate_toolchain_backend_supported` and the two `make`-quality-check gates in `validate_pipeline_semantics.py`, the `make ∧ fortran` authorship conjunction in `workflow_conductor.py`, and `codegen_bundle.LANGUAGES` / `LANGUAGE_EXTENSION_ALLOWLIST` / `COMPILER_SELECTOR_FAMILIES_BY_LANGUAGE`. Until these consult the registry, "add a backend without editing a gate" is not true.
      - **NOT closed by this, and deliberately:** `MAKE_QUALITY_CHECK_REQUIRED_LANGUAGES = {fortran, c, cpp, mixed}` stays in the neutral core. It is a POLICY set over language families, not knowledge a backend owns.
      - **A design correction, recorded so it is not undone.** Routing these sites through "is this value implemented?" is FAIL-OPEN: the question a gate has is whether the neutral core does this named job for this value, which is what `provides(axis, value, capability)` answers.
    - **OPEN — `language/fortran` source reading, `validate_pipeline_semantics.py`.** `_fortran_procedure_envelopes`, `_iter_fortran_calls`, `_fortran_source_views`, `_local_fortran_module_map` and the `_FORTRAN_*` regex family. The §5.1 line and stanza layer already moved to `backends/language/fortran/lines.py` (2026-08-15).
    - **OPEN — `build_system/make`, `workflow_conductor.py`** plus the GNU-make sublanguage parser in `validate_pipeline_semantics.py`: who authors `src/Makefile`, what its grammar is, and what a relink or a case invocation looks like.
    - **OPEN — `docs` — the phase and contract documents.** `docs/workflow/phases/phase_02_generate.md`, `docs/workflow/CHECKS_MODULE_CONTRACT.md` and `docs/workflow/CODEGEN_BUNDLE_CONTRACT.md` carry the bulk; `docs/BACKEND_BOUNDARY.md` §Placement is canonical for where the technology-specific half of each goes.
    - **OPEN — `skills`.** `skills/workflow-generate-generate/SKILL.md` and `skills/workflow-generate-verify/SKILL.md` carry `fortitude` rule ids and Fortran spellings a leaf reads directly.
    - **OPEN — `language/fortran` source reading, `orchestration_runtime.py`.** `_FORTRAN_SUBROUTINE_RE`, `_extract_subroutine_interface`, `_parse_fortran_dummy_declarations` and the `_FORTRAN_*` family, read by the launch-prompt renderer rather than by a gate.
    - **OPEN — `prompt templates`, `tools/prompt_templates/`.** `fortitude` rule ids, the `implicit none` trap, `-std=f2008`, the neutral-to-Fortran lowering table. These move to `tools/prompt_templates/backends/<axis>/<backend_id>/` and are composed into the leaf prompt; the pinned-hash prompt/gate drift guard has to be extended to cover the fragments.
      - **A second carrier in the same area** (issue #142, 2026-09-03, Codex): the pure `generate.verify` reviewer's context reads `CHECKS_MODULE_CONTRACT.md` unconditionally (`workflow_conductor._build_pure_verify_context`), not through the registry, and the producer half reads `src/<spec_id>_runner.f90` — a file extension the axis value implies — from the neutral core. Migrate it WITH the templates; direction is to resolve both through the same registry capability that gates the dispatch.
    - **OPEN — `tools/hooks/common.py`.** The source-extension allowlist (`.f90 .f .f95 .f03 `.f08` `.fpp` plus the C family) and the compiler-byproduct extensions. Small, and the one area where the extension list is a SECURITY boundary, so it moves last and keeps a fail-closed default that admits nothing when no backend answers.
    - **CLOSED areas, one line each.** Structure, rule and ratchet (2026-08-14). Capability dispatch — `unsupported_reason` / `unavailable_reason` / `unimplemented_reason` / `provides` (2026-08-14). `language` tables in `codegen_bundle.py`, all but the `.mod` / `.o` build-graph flattening rules, which stay open above (2026-08-14). `language/fortran` runner emission, now `tools/backends/language/fortran/runner.py` (2026-08-15). The §5.1 line and stanza layer (2026-08-15). The `linter` side of the `compiler` + `linter` adapters (2026-08-27 issue #111, completed 2026-09-01 issue #120); the `compiler` side is not, and is the open area below.
    - **OPEN — `compiler` adapters, `mcp_servers/` and `tools/hooks/syntax_evidence.py`.** `_SYNTAX_COMPILER_ADAPTERS`, `_gfortran_syntax_argv` and the syntax-evidence reader still spell one compiler's argv and diagnostics in the neutral core. The `linter` half is done: the `fortitude` / `cppcheck` / `ruff` rows left the server for `tools/backends/linter/<id>/`, and `_INLINE_LINT_PRESET_COMMANDS` is deleted.
      - **The sampled ratchet does not measure this migration, in either direction.** The token class counts the NAME, which the neutral core may still use as an opaque axis value, so a migration can leave the count unchanged and a legitimate mention can raise it. Read it as a bound on growth, never as a progress measure.
  - **A gap that cannot be closed, recorded because four review rounds were spent trying**: `provides` answers a DECLARATION question — does this repository do this named job for this value — and no question of that form can answer whether the ANSWER is correct. The check bounds where knowledge lives; it does not verify what the knowledge says.
  - **A forward cost this creates, named here because nothing else says it**: the validator's `try` / `except ModuleNotFoundError` bootstrap spells every module-level import twice, so a new backend import is two edits and a missed second one fails only on the path that takes the fallback.
  - **Cross-axis residue, recorded rather than closed**: `MAX_RENDERED_LINE = 100` in the moved module is the `linter` axis's column limit (fortitude S001), not a language fact.
  - **Standing limit of the check, not a defect to close.** An assertion cannot witness its own weakening — an `assertEqual` relaxed to a containment, a necessity test relaxed to a tautology — because the weakened form is what would run. Five decisions in `tools/tests/test_backend_boundary.py` are of that kind; they are covered by external mutation runs and by review, and the module docstring says so rather than letting them read as covered.
  - Related, outside this item: the `harness_fortran_cpu` `controlled_spec` names Fortran spellings in the prose around its §5.1 block. `spec/` is out of this rule's scope because `docs/CONTROLLED_SPEC.md` owns the language-neutrality requirement for a `spec`; closing the gap belongs there.
  - Not part of this item: the `LLM backend` axis, already separated correctly (`tools/llm_config.py` + `llm.yaml`) and the precedent this rule is modelled on.

- **Two residual holes in the `Generate.gate` lint file set, and one linter-version boundary** (2026-08-28, medium, issue #111 — reproduced, pre-existing). Three open, none introduced by the change that found them. (1) **Neither `Generate.gate` content check sees the linter's default-excluded subdirectories** (`build/` `dist/` `venv/` `_build/` `site-packages/`), and both syntax-stage readers walk with `iterdir()`, non-recursive; closing it is ONE flag, `--exclude=`, not a file-set declaration, and it is deliberately not taken for `fortitude` by the operator's decision because that is the one linter a live certification gate reaches. (2) **`S001` compares `>=` on 0.8.x and `>` on 0.9.x**, so a line of exactly 100 columns fails on the declared floor build and passes on the ceiling ones; the renderer's own guard rejects `> MAX_RENDERED_LINE` rather than `>=`, so it emits exactly-100 lines, and the fix is wrapping in the renderer, not tightening the guard, which would fail closed on the one node whose runner a leaf authors. (3) **The launch version arm asks about the DEFAULT language's linter**, so a host with an out-of-range `fortitude` is refused for a run that would never invoke it. Evidence: measured on fortitude 0.8.0 / 0.9.0 / 0.9.2 — the declared invocation over a tree holding `top.f90`, `build/probe.f90` and `dist/probe.f90` scans `top.f90` alone and reports 5 findings, and with `--exclude=` scans all three and reports 15; zero instances of either condition exist today (`os.walk` over all 352 `src/` directories finds no Fortran file under an excluded directory, and every `spec.ir.yaml` declares the same language), so both are bounds on growth. Out of scope and recorded rather than closed: the linter version is checked at launch and never again, which is operator-reachable only. Record: https://github.com/seiya/atmofab/issues/111#issuecomment-5560396526 .

- Once the Claude backend can obtain `session_id` or `agent_session_id` from the hook payload, abolish the `agent_run_id` resolution that depends on `active_child_agent_run_id.txt`, and unify it to the same session-identifier-based resolution as the Codex backend. Removal targets: the active-file management helpers and Claude-specific branches in `tools/orchestration_runtime.py`, the active-file reference branch in `tools/hooks/cli.py`, and the tests that assume the active file. Completion criterion: `agent_run_id` resolves uniquely from the session identifier on both backends, with tests showing no active file is generated.

- Harden `Compile.generate` so it reliably authors full-fidelity `io_contract.test_predicates`, reducing the run-to-run variance where it emits degenerate predicates and forces a full node re-roll. **The prompt-side half is what is open** — the deterministic `Compile.static` gate that rejects the all-bare-`verdict.overall eq pass` shape landed 2026-07-18. Fix direction: strengthen the `Compile.generate` prompt and exemplar so per-check `op`/threshold conditions, `per_case` value maps for nx-dependent thresholds, `case:`-scoped conditions for cross-case reductions and the matching `io_contract.diagnostics_contract.metrics` addresses are the default authoring. Keep it separate from the Z2 pure-executor work and do not smuggle a `spec_version` bump through it. Completion criterion: a representative multi-check node authors them across several consecutive rolls without a V3 fidelity `major`. Evidence: observed on `orch_20260717T154247Z_d82d283a` (`compile.verify` attempt 2, `reason_detail=dev_verify_major`, repo `df0b6ad`); the immediately following re-roll authored correct predicates and passed, which is what makes it variance rather than incapacity. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5559625259 .

- (Optional) A **flow-sensitive / interface-aware deterministic check for `semantic_dependency.required_sources` reachability**, if deterministic coverage of that property is ever wanted again. Check 2 was REMOVED and `Generate.verify` **G5** owns the property; this records the decision so the removal is not re-litigated. Building it needs a real dataflow pass — per-argument intents resolved from each dependency operation's published interface, plus reaching-definitions over statement order — because chain-threading and dead-write overwrite are the same syntactic shape with opposite meaning. `gfortran -fdump-fortran-original` is ruled out as the source: comparing 12.4.0 and 14.2.0 over the 365 in-tree models plus 41 adversarial fixtures, the `code:` section is exactly where the two disagree (27 of 319 analysable files), so a verdict read from there depends on which compiler the machine has. A named reproduction is waiting for whatever the pass becomes: the `profile__dynamics_shallow_water_profile_2d_rusanov_p0_ssprk2` model under `workspace_20260712/` yields `candidates=['flux_ok', 'u_new', 'z_b']`, a false "does not propagate" that is producer-side for two of the three and consumption-side for the third — latent only because the gate returns for any `node_key` outside `problem/`. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560396801 .

- **A source needing statement labels both kept and stripped is refused** (2026-08-14, low — reproduced, declared residue of the front-end swap). Fail-CLOSED and pinned as refused (`test_a_file_needing_both_label_readings_at_once_is_refused`). Closing it means choosing the label reading PER STATEMENT — an error-guided repair loop, strip the labels the parser reports errors on and re-parse, bounded — rather than a fourth rule about which labels matter; three such rules were written and each was defeated by a legal program in the next review round. Evidence: 0 of the 365 in-tree `*_model.f90` carry a statement label of any kind, so no model needs either reading today. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560466309 .

- **A never-called contained procedure silences the `problem` model gates** (2026-08-14, low — reproduced, pre-existing on both revisions). **The one-line fix is known and is known to be WRONG**: cutting `body` at `contains` was tried during the 2026-08-12 envelope work and reverted, because it makes a `call` inside a contained procedure, with the `intent(out)` in the host, land in an envelope with no `intent(out)` at all — fail-OPEN, and a regression against `origin/main`. Both directions are documented in `_FortranProcedureEnvelope`'s docstring. The honest closure needs the flow-sensitive pass recorded above, which can tell a dead procedure from a called one. Evidence: 0 of the 365 in-tree `*_model.f90` define a contained procedure at all. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560466654 .

- **What the launch-time host probe does NOT cover** (2026-08-27, low, issue #109 — recorded with the fix rather than left implicit). Two limits stay open: the probe resolves the DEFAULT selection rather than a node's IR, so an IR that pins `impl_defaults.toolchain.compiler` has its BUILD compiler unprobed; and a `--with-deps` closure is not walked, the target node's selection standing for the closure. Both are narrow while `_validate_toolchain_backend_supported` pins every node to the same pair. The mid-run gates stay as the backstop — this is an earlier detector, not a replacement. Evidence: the mandatory syntax stage is `DEFAULT_COMPILER` whatever the IR says, and a skipped mandatory stage is a `Generate.gate` fail_closed rather than a silent pass. Record: https://github.com/seiya/atmofab/issues/109#issuecomment-5560397095 .

- **`run_syntax_check`'s missing-compiler `skipped` is closed under the workflow and open outside it** (2026-08-27, low, issue #109 — recorded because it was raised as a fail-open and did NOT hold). Kept because the misreading is easy to repeat: the permissive return is in the TOOL and the closure is in its only workflow caller, one file away, so a reviewer who reads the server alone reaches the wrong conclusion, as one did. Evidence: `tool_run_syntax_check` returns `{"ok": true, "skipped": true}` when `shutil.which` is None, and `_gate_syntax_check` raises `generate.gate syntax check: mandatory gfortran stage unavailable` for a skipped MANDATORY stage, which surfaces as a `deterministic_generate_error` transport fail_closed; an OPTIONAL stage is skipped on purpose, which is what the tool-side return exists for. Record: https://github.com/seiya/atmofab/issues/109#issuecomment-5560466955 .

- **A stale certified IR is detectable at launch but surfaces mid-run, burning one billed `Generate.generate`** (2026-08-21, medium — the asymmetry measured, the cost not). Both inputs are readable at launch — the closure IRs and the `controlled_spec` §5.1 the freshness check already resolves — and the precedent is the rc-3 check sitting a few lines above in the same file. Keep the gate's fail-closed as the backstop if this is done: a `--with-deps` run can re-certify between launch and the gate. Evidence: `validate_pipeline_semantics` answers rc 3 and rc 4 for the two conditions no leaf can repair and both route terminally, but only rc 3 has a launch-time counterpart (`run_workflow.REQUIRED_PYTHON_MODULES`); rc 4's condition is first seen at `Generate.gate`, after `Generate.generate` has run, so the run shape that produces it spends one billed leaf per occurrence before the terminal verdict. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560397391 .

- **`classify_post_judge_violations` still decides a DISPOSITION by scanning violation text** (2026-08-21, low — **reachability NOT verified**). **What must be established before anything is written or deleted**: whether any `--stage pre_judge` violation interpolates a path a leaf chose. If none does, this is unreachable by construction and the right answer is a comment saying so, not a channel change — do not classify it either way from this entry, run the attack. Direction is fail-CLOSED only: forging `recoverable` needs the basename `semantic_review.json`, the judge's own single deliverable, which buys a leaf nothing. Evidence: `classify_post_judge_violations` takes each violation bullet's leading token, reduces it to a basename, tests it against two frozensets, and applies three substring tests — the two `_POST_JUDGE_UNRECOVERABLE_MARKERS` and `"orchestrations/" in token`. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560397672 .

- **`_matches_phase_contract` is a membership allowlist where its own name says required set** (2026-08-13, medium). Fix direction: key the expected set on `(agent_role, step, substep)` and require set EQUALITY per substep, which is meaningful for the compile substeps and the validate gate substeps only — `generate` / `build` / `validate.execute` name files whose ids vary per run, so those branches stay membership tests and the change must say so. It needs ONE named constant per substep rather than a test, because until the set has a single definition a test outside the function can only SAMPLE names it must reject. Under-declaring costs the declarer write authority rather than gaining any, so this is a liveness gap, not a bypass. Evidence: three demonstrated escapes, all green against both existing pins — one more literal name (`<ir_ref>/summary.md`), a subtree prefix (`<ir_ref>/scratch/`), and an extension family (`path.endswith('.log')` at the IR root) — and three review rounds each broke the previous sample's shape. `compile.static`, `generate.gate`, `validate.pre_judge` and `validate.post_judge` have no captured payload under `tools/tests/data/conductor_launch_requests/`, so the test must capture one or say it is synthesized. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560397992 .

- **`_detect_bash_write_commands`' substitution recursion is exponential in nesting depth.** `tools/hooks/cli.py`: the top-level `_extract_command_substitution_bodies` already flattens every nested body and the recursion then re-extracts from each one, so a synchronous hook that runs on every tool call has a stall path. **Not reachable and not a `leaf shortcut`** — a leaf gains nothing by nesting substitutions. Directions: pass the already-flattened body list down instead of re-extracting, or bound the body count. Evidence: measured on issue #74's branch with `"echo " + "$(echo "*d + "hi" + ")"*d` — depth 10 = 0.022s, 14 = 0.365s, 16 = 1.458s, 18 = 6.242s, roughly 4x per level on a 151-character command; no construct of that shape exists in this tree's leaf corpus (283 commands) or in an operator session corpus (51,425).

- **`_detect_sed_inplace_targets` mis-reads the two `sed -i` spellings a leaf actually writes.** `tools/hooks/cli.py::_looks_like_sed_script` recognises only a `/`-delimited script, so (a) any other delimiter — `sed -i 's|old|new|' <path>` — reports the SCRIPT as the write target and never examines the file, and (b) `sed -i -e '<script>' <path>` and `sed --in-place ...` report NOTHING, so `validate_write_access` never runs. **(b) is the `leaf shortcut` route**: `sed -i -e 's/xfail/pass/' <managed pipeline artifact>` is a gate edited rather than satisfied, with the write guard recording nothing. Direction: read the delimiter from the token — it is whatever character follows the command letter — and treat `-e` / `--expression` / `--in-place` as the option grammar they are. Evidence: measured identical on `5f254ef` and on issue #74's branch, so pre-existing; a census over 51,425 unique operator Bash commands finds 104 rows where a `sed` script is named as a write target and 10 carrying both `-i` and `-e`.

- **The Bash write-target extractor reports a destination it cannot resolve, and that refuses legitimate work.** `tools/hooks/cli.py::_detect_bash_write_targets` returns a `$VAR` destination unexpanded, a glob unexpanded, and a destination relative to an earlier `cd` un-anchored, so each fails the manifest check and blocks a write the leaf was entitled to make, naming a path that is not the one being written. Directions: anchor the write side on `cd` with the read side's own resolver; and for a destination carrying `$` or a glob metacharacter, decide between reporting nothing and refusing with a message that says the destination could not be resolved — do not leave it reporting the unexpanded token, which is the worst of the three because the block names a path nobody wrote. Evidence: all three measured identical on `5f254ef` and at HEAD, and they live on the REDIRECT branch, the only branch that names a path at all; the read side already anchors on `cd` and declares `$VAR` operands as residue, so the two halves of one module disagree today. Record: https://github.com/seiya/atmofab/issues/74#issuecomment-5560398293 .

- **Close the medium-severity enforcement findings from the 2026-08-10 audit** (2026-08-10 audit, medium, issue #74): (a) a missing/unparsable capability `expires_at` skips the expiry check in both `validate_mcp_build_tool_invocation` and `_validate_run_gate_permissions` — reject instead; (b) the `run_program` command constraint is a substring test (`"spec.ir.yaml" not in joined`) — anchor it to an actual argv element; (c) bwrap write-root binding resolves through directory and parent symlinks, only leaf file pins are lstat-checked — verify the bound root and its parents; (e) `orchestration_id` bare-component validation exists only for the codex feature cache path, and `_extract_orchestration_id` resolves payload-FIRST, so a hook-payload value outranks the host-set env var — validate at that resolution point or flip it to env-first; (f) `record_launch` appends the session-run-index row before prompt validation, so a raise leaves an orphan `running` row, and its Claude serial-launch guard is check-then-write with no lock across the long body. **(d) is closed** — the Bash write-command refusal — and its record, with the three review rounds that produced it, is at the link below. Completion criterion: each of (a)-(f) has a deterministic test demonstrating the closed behavior, or a recorded decision that the gap is intentionally left to another layer. Record: https://github.com/seiya/atmofab/issues/74#issuecomment-5559632363 .

- **Fix the conductor-layer robustness findings** (2026-08-10 audit, medium, issue #142, grouped): (a) `_classify_leaf_infra_error` misses the Python/urllib spellings — a socket `timed out` and `[Errno -3] Temporary failure in name resolution` classify as non-retryable and fail the run in one shot, while the Node spellings retry; record too whether `response_deadline_exceeded` / `response_too_large` are intentionally non-retryable. (b) is PARTLY done: the `_run_pure_verify_substep` call site is wrapped and the `CHECKS_MODULE_CONTRACT.md` slice fails closed, and what remains is the four NODE artifacts still degrading to `""`. (c) `substep_start` emits the substep ordinal as `attempt` — emit the real attempt count. Completion criterion: classification tests pin the Python spellings as retryable; verify-side missing-document tests fail closed before launch; run-log `attempt` semantics are uniform and tested. Evidence for (b): an empty string never reached a billed leaf — `_validate_pure_launch_request_payload` requires a non-empty string for every `PURE_CONTEXT_REQUIRED_KEYS` entry at `record-launch` and `Conductor.runtime` turns that refusal into a `RuntimeError`, so today's failure mode for an empty node artifact is an UNRECORDED conductor crash, not a billed launch. Record: https://github.com/seiya/atmofab/issues/142#issuecomment-5560398588 .

- **Remove the dead apply_patch gate cluster and consolidate the mirror gate/validator pairs** (2026-08-10 audit, medium). Dead code presenting as live defense: `_reject_noncanonical_phase_write`, `_write_phase_authority_violation`, `_resolve_current_phase_state`, `_phase_write_requires_child_running` and `PHASE_ARTIFACT_GUARDED_PREFIXES` in `tools/orchestration_runtime.py` have no callers; the orchestration tail of `_validate_actual_write_paths` is unreachable; and two documents describe the retired gate through `_validate_apply_patch_gate_coverage`, which does not exist. Mirror pairs whose one-sided edits diverge gate and validator: `_validate_run_gate_permissions` ↔ `validate_mcp_build_tool_invocation`, and `_preflight_allows_agent_launch` ↔ `_validate_preflight_payload`. Fix direction: delete the dead cluster and the stale doc references, and extract each mirror pair's shared body into one helper both sides call. Completion criterion: a repo-wide grep finds no reference to the deleted names, each pair shares one implementation, and the existing gate/validator tests stay green.

- **Replace the static skip guard with a runtime skip inventory** (2026-08-13, medium). Fix direction: run the suite and compare the skips the runner ACTUALLY reported against `_DECLARED_ENVIRONMENT_SKIPS`. That needs no Python semantics, so every evasion the static guard's seven review rounds found closes at once, and it reaches the routes no reading of skip syntax can (`__test__ = False`, a test defined under an `if`, `del test_f`). It cannot be a test inside the suite it must run, so it is a `tools/` entry point or a CI step; the CI precondition landed 2026-09-06 (issue #161 PR-3) and the job already passes `-rs`. Completion criterion: a check that fails when any reported skip reason is absent from the table, driven by a real suite run, with the table having exactly one definition. Evidence: the runner's skip set is currently EMPTY — the sandbox skips were closed by lifting `kernel.apparmor_restrict_unprivileged_userns` rather than accepted — so an inventory built against today's runner has no positive example and the fixture has to construct one. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560398900 .

- **`mcp_servers/mcp_call.py` has no timeout of its own, and a silent server hangs it for ever** (2026-09-06, low, issue #161 — measured, PRE-EXISTING). A server that reads stdin and writes nothing leaves `mcp_servers/mcp_call.py`'s `_read_message` blocked in `readline()`: no exception is raised, so the diagnostic path never fires, and the gate-verification command `docs/RUNBOOK.md` §refusals and `.claude/skills/atmofab-enforcement-change/references/verification.md` teach simply never returns. Fix direction: bound the read — `selectors` on the pipe, or a deadline the client enforces — and report the timeout with the same `produced:` suffix the other failures carry. Evidence: `timeout 20 python3 mcp_servers/mcp_call.py …` against such a server exits 124; `_read_message` is byte-identical at `9f2e16d`, so this is not a regression.

- **Found while testing this, out of PR-2's scope and recorded rather than pulled in** (2026-09-06, low, issue #161). Two PRE-EXISTING classes in `tools/tests/test_build_runtime_server.py` — `OrchestratedEnvAllowlistTests` and `RunLinterPresetDispatchTests` — depend on the suite's own environment and do not use `_StandaloneServerEnvMixin`, which exists for exactly this. Fix direction: give those two the mixin, or build the environment they reason about in their own fixtures. Evidence: under `python3 -m unittest` (which does not load `conftest.py`) with `ATMOFAB_WORKFLOW_MODE=1 ATMOFAB_ORCHESTRATION_ID=orch-x` set in the shell they report 1 failure and 5 errors; under pytest they pass, because `conftest.py` strips ambient `ATMOFAB_*`.

- **Sweep the documentation drift found by the 2026-08-10 audit** (2026-08-10 audit, medium, checklist). Thirteen drifted statements, each a retired name or a contract example that no longer validates: `AGENTS.md`'s `Generate.syntax`; `LAUNCH_PROMPT_REFERENCE.md`'s `plan` token and its `execute`/`judge` steps; `phase_04_validate.md`'s retry table keyed on `failure_class` values the host no longer produces; `CODEGEN_BUNDLE_CONTRACT.md`'s JSON example, which `validate_bundle` rejects as written; `AGENT_CONTRACT.md`'s nonexistent `_validate_workflow_bash_policy` and its underscore-prefixed `validate_write_access`; `run_workflow.py`'s `--wait-usage-reset` argparse help; `MCP_COMMAND_LOG_PLACEMENT.md`'s pre-rename path segment; `GLOSSARY.md` and `TESTS.md` on `dependency.resolved.yaml`; `WORKSPACE_LAYOUT.md`'s reservations path; `shape_expr.schema.json`'s `^...$` anchors, which `SCHEMA.md` forbids; a profile `tests.md` pinning a version the catalog does not carry; the two prompt templates justifying the launch-prompt prohibition via a nonexistent `Agent` tool; and the no-op `{{ACTOR_ROLE}}` substitution. Plus one that is a `spec` change rather than a doc fix, which is why it was left: the retired `case.resolved.yaml` survives in 12 `spec/**/controlled_spec.md` / `tests.md` files. Completion criterion: each is fixed or explicitly recorded as intentional; where a class recurs, prefer extending the doc-sync test family over a prose-only fix. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560399218 .

- (Optional, larger) **Decompose the two outsized modules along the seams the 2026-08-10 audit identified** (structural). `tools/orchestration_runtime.py` (21.8k lines, ~400 top-level definitions, a ~1,100-line `main()`) and `tools/validate_pipeline_semantics.py` (13.4k lines) each hold six to nine domains with one-way dependencies. Seams: Fortran interface extraction, JSON transaction/lock infrastructure, capability/write-roots gates, bwrap profiles, FS-diff write validation, preflight/probe, launch-prompt rendering, and records+CLI for the runtime; stanza parsing/normalization, Makefile analysis, runner static gates, and orchestration hierarchy validation for the validator. A split also converts the cross-module private-name imports into explicit public APIs. Completion criterion: not scheduled — recorded so any future decomposition follows these seams instead of ad-hoc extraction.

- **The codex `hooks` preflight check measures a configuration the leaf does not use** (2026-08-21, medium — measured, latent). Fix directions, either or both: have `_prepare_codex_workflow_home` write `[features] hooks = true` into the isolated `config.toml` so the leaf's configuration is declared rather than inherited, and/or run the feature probe with `CODEX_HOME` set to a prepared home. Latent, not live: the CLI default is `true`, so the gate's verdict happens to be right today; the failure is a release whose default flips, after which the run launches and the leaf comes up with no read guard, no write guard and no capability gate — fail-open and silent. Completion criterion: the value of `hooks` a leaf runs under is decided by a file this repository authors, with a test driving the probe against that file. Touching `_prepare_codex_workflow_home` is an enforcement-machinery change. Evidence: measured on codex-cli 0.147.0, `codex features list` with `CODEX_HOME` at the operator's home -> `true`, an EMPTY directory -> `true`, a home carrying only `[features] hooks = false` -> `false`, and a copy of what `_prepare_codex_workflow_home` actually writes -> `true`; `docs/RUNBOOK.md` §0-3 carries the measurement. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560399479 .

- **`WritePreflightConcurrencyTests::test_concurrent_preflight_and_mark_do_not_clobber_verified` fails intermittently in a full-suite run** (2026-08-21, low, issue #185 — observed, root cause NOT investigated). Not diagnosed. The two plausible directions, neither checked: contention on a shared temporary root under load, and a real race in the preflight write path that only widens when the machine is busy — which is what the test is about, so the second would make the flake the finding rather than the noise. Danger of leaving it: this repository's review procedure tells a reviewer to compare a suite DELTA against a baseline, and a row that fails at random makes that comparison unreliable in exactly the direction that hides a real regression. Completion criterion: the failure is reproduced deliberately and either fixed or shown to be a fixture-contention artefact with the fixture corrected — not "ran it N times and it passed", which is not a non-existence proof. Evidence: observed twice in full-suite runs on the same host, several runs apart; it passed on the next full-suite run both times and three of three when run alone, so it needs the rest of the suite running beside it. Record: https://github.com/seiya/atmofab/issues/185#issuecomment-5559633550 .

- **Four mechanisms of the Bash credential guard are unobserved by the credential-guard suite** (2026-09-02, low-to-medium, issue #84 — measured over 26 mechanism mutants). Four mechanisms of `tools/hooks/common.py`'s Bash credential guard can each be deleted with `tools/tests/test_hooks_common.py` fully green: the raw-command marker-regex scan that catches `~` / `$HOME` / `${HOME…}` / `<abs-home>` spellings when punctuation mangles tokenization; the resolve-budget anchor truncation; the `lexists` / `.resolve()` symlink fallback, the only defense against a symlink planted inside the rw-bound `~/.claude`; and `${V/#pat/rep}` / `${V/%pat/rep}` anchored substitution. The route is the next refactor of that module: it can remove any of them and the guard's own suite reports nothing. Two are cheap to witness; the anchor truncation needs a command that reaches it at all, which a reviewer probed and could not construct — that one may be dead code rather than an unwitnessed mechanism, and the census question is which. Evidence: deleting the marker scan outright gives 364 passed on `fc821b7` and on the branch alike. Scope of the measurement: green was read over `test_hooks_common.py` only, so a mutant killed by another test file is not excluded for three of the four. Record: https://github.com/seiya/atmofab/issues/84#issuecomment-5560399772 .

- **The claude read-side preflight gate has no required-check-name set, and the roster check has no launch-time backstop** (2026-08-22, low, issue #71 — measured; ATTEMPTED and backed out, with the measurement as the reason). Every claude check is ANDed into `can_launch_agents` at the moment it is probed, which says nothing about a `preflight.json` produced by OLDER code and read back by `_require_preflight_launchable` on a resume or with the live probe disabled: such a document carries no `claude_leaf_tool_roster_classified` row at all, and `status=pass` alone launches a run whose leaf tool set was never measured. It matters more here than for the sibling checks because the conductor cannot see a roster — only the endpoint can. Evidence: the mirror (`CLAUDE_REQUIRED_LAUNCH_CHECKS`, required PRESENT and not-false rather than `is True`, because two rows are legitimately `null` on the advisory-skip path) was written and MEASURED — it took the suite from 5120 passed to **56 failed**, because that many fixtures build a claude preflight payload without those rows. The fixtures are a proxy for the older documents the gate would refuse, so the blast radius is the finding: this belongs in its own branch with the fixtures migrated deliberately. Record: https://github.com/seiya/atmofab/issues/71#issuecomment-5560400029 .

- **The `Glob` pattern check now DEPENDS on a measured vendor behaviour, and nothing re-measures it** (2026-08-25, medium — a deliberate narrowing, recorded so the premise is re-checkable rather than forgotten). The check refuses a pattern beginning with `/` or with `~` — `/` because that is the one shape MEASURED to reach outside the tool's `path`. **If a future CLI resolves `..`, follows a symlink, or expands a brace alternative before testing `isAbsolute`, the relative escape reopens and nothing in this repository notices**: the preflight roster check measures WHICH tools a leaf gets, not what one of them can reach, and `test_a_relative_pattern_is_not_refused_however_it_is_spelled` will still pass, because it asserts the current, deliberate non-refusal. To re-measure: `python3 .claude/skills/atmofab-enforcement-change/scripts/measure_claude_tool.py`, committed for exactly this — each row DECLARES whether it must read or must be inert, the script compares and exits non-zero on any disagreement, and `tools/tests/test_measure_claude_tool.py` pins the case list's coverage, the fixture's saturation, the detector in both directions and the verdict. An INERT row that reads is the end of the premise; a CONTROL row that reads nothing means the measurement broke and nothing above it can be believed. Evidence: driven against the real tool on CLI 2.1.239 in a saturated fixture, five absolute spellings read what they name and every relative spelling — `../`, `*/../../`, a brace with an absolute alternative in either order, a symlinked directory and file, `~/…`, `$HOME/…`, a leading space, a leading tab — returns nothing. Record: https://github.com/seiya/atmofab/issues/71#issuecomment-5560400322 .

- **Issue #71's narrowed `Glob` pattern check keeps three accepted residues, none of them an escape** (2026-08-25, low, issue #71 — recorded because this repository's convention puts residue here). Two remain and neither admits a read the manifest does not grant. (1) An absolute brace pattern spanning two granted roots is refused although it reads: `<repo>/{docs,sub}/*` has the literal prefix `<repo>/{docs,sub}`, because `{` is deliberately not in `_GLOB_META_RE` — the Bash extractor that shares it resolves against a filesystem where a brace is not a wildcard — so it is under neither grant. Closing it needs the brace expander deleted in round 12, whose four guards were where three review rounds found defects; absent from the corpus, and the refusal names the working alternative. (2) A `~` pattern is refused although it is inert: the tool asks `isAbsolute`, which `~` is not. Kept because `_glob_literal_prefix` already returns the expanded location, so the condition costs one character, and the refusal text says so rather than calling it absolute. Evidence: the third residue was CLOSED 2026-08-25 as never-open — the harness committed in the same commit already carried a tab-prefixed row, so the residue was sampled before it was filed as unsampled; `test_measure_claude_tool.py` now pins both whitespace prefixes into the default case list. Record: https://github.com/seiya/atmofab/issues/71#issuecomment-5560400591 .

- **A reviewer measured a different built-in roster than this branch records, on the same CLI version and host, and the disagreement is unexplained** (2026-08-25, low — measured, unreconciled). **Operational consequence**: a `claude_leaf_tool_roster_classified` failure naming an unfamiliar built-in may be roster nondeterminism on one CLI version rather than a vendor change, and `docs/RUNBOOK.md` §0-2's remedy does not admit that possibility — re-measure with the capture harness before treating such a failure as a change to classify. Evidence: `CLAUDE_LEAF_TOOLS` records 23 built-ins unfiltered and 19 under the old denylist argv; a round-13 reviewer measured 27 and 23 on CLI 2.1.239 (the extra four being `TaskCreate` / `TaskGet` / `TaskList` / `TaskUpdate`) and an earlier one 24/20 with `Skill`, while three re-takings under the LEAF's argv and an independent round-15 re-take with the production capture server over three argvs all give 23/19. Every request the CLI made to the loopback stand-in was logged — two POSTs, no GET — so it is not a startup fetch. The enumerated list is exhaustive only for the argv it names, which is what keeps it true. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560400857 .

- **The orchestration read audit (`access_logs/<agent_run_id>.jsonl`) is observational, not tamper-proof, and no rework is scheduled** (operator-confirmed 2026-06-26, low — future direction, not started). Two directions were named and neither is started: (a) move the audit append out of the sandbox, host-side, which needs a gate-to-host round-trip channel that does not exist today; (b) remove the ro-bind direct-read path entirely and force every read through the gate, which is large and separate. No issue tracks either. Authorization is unaffected — `log_orchestration_read`'s allow/deny check runs independently and first — so this is an integrity-of-the-record gap, not an access-control one. Evidence: a leaf reading through a bwrap ro-bind mount hits `run-gate --gate orchestration_read` only when the path is outside the mount, so most reads never reach the gate that would log them; and the audit line is appended by the leaf's own `run-gate` subprocess inside the sandbox, over a path the per-file write grant makes leaf-writable. `docs/HOOKS.md` and `docs/ORCHESTRATION.md` §"Enforcement surface" already state the threat model; what neither records is that this was a deliberate accept-for-now. Record: https://github.com/seiya/atmofab/issues/181#issuecomment-5560401150 .

- **Issue #112 landed the gate's finding-attribution rule; three things it does NOT close are recorded here** (2026-08-28, issue #112). Four residues, none of which the rule closes. Record: https://github.com/seiya/atmofab/issues/112#issuecomment-5560401432 .
  - **The syntax checker cannot decide an unattributable finding, deliberately.** The host-rendered runner `use`s the leaf-authored checks module, so a diagnostic at a runner line can be the LEAF having missed the fixed checks ABI — warm-repairable — and terminalizing it would convert a fixable defect into a permanent `fail_closed`. Its probe records `checkers.syntax.attribution` and routes nothing; the `unattributed_interaction` count is what a later change should decide on.
  - **The Makefile arm is not implemented.** The STATIC arm wraps only the runner gates, so a post_generate finding against the host-authored control file still warm-retries. Left out because `_conductor_authors_makefile` is strictly WIDER than `_conductor_authors_runner`, so the validator's `_execution_m3c_language` mirror is the WRONG oracle and would under-attribute — a fail-open. Closing it means extracting the control-file capability loop into its own named predicate, with a row in the conductor-validator parity test.
  - **`lineage.json` is a fourth host-authored surface at post_generate and is not covered.** `_validate_pipeline_lineage_presence` flags a malformed `pipeline_id` / `node_key` binding in a file the conductor writes, on EVERY node rather than only an M3c one, so its attribution helper is a different one again.
  - **The lint attribution probe's composite-preset branch is unwitnessed, and unreachable today.** `registry.unsupported_reason("language", "mixed")` is non-None, so a mixed node is rejected at `Compile.static` before any of this runs.
  - **The premise the whole lint arm rests on, and nothing re-measures it automatically**: measured 2026-08-28 on fortitude 0.8.0 over the real `src_20260827_002` tree of issue #110's node, full = 3 findings, leaf-only = 2, host-only = 1, with the host probe producing nothing of its own. A linter that resolved across files would break the partition.
  - **Blast radius, measured, so it is not discovered later**: 42 of 45 sampled archived source trees under `workspace*/` have a FAILING host probe, because runners rendered before issue #111 carry an `! allow(C003)` directive the current renderer no longer emits. That is the intended semantics, and it means a future renderer lint regression terminalizes EVERY node on its first gate failure. Not verified: `ruff` was not installed on the machine this landed on.

- **`docs/workflow/phases/phase_04_validate.md` contradicts itself on how many `--stage pre_judge` exit codes are answered before violations are classified.** Line 20 says "Three dedicated exit codes (3 `static_frontend_unavailable`, 4 `stale_dependency_ir`, 5 `host_authored_artifact_violation`)"; line 81 of the same file says "**Two** exit codes are answered before the bullets are read at all" and lists only 3 and 4. `docs/ORCHESTRATION.md`, `docs/RUNBOOK.md` §Validate and `_post_judge_inproc`'s `terminal_category` map all say three, so line 81 is the likely stale one — but this was not established, only observed, and deciding which sentence is right is a separate reading of the gate. Found by a review round on issue #114 (2026-08-28); pre-existing.

- **Left open by PR #140, filed rather than fixed: the private-root resolvers are not TOTAL for a `~account` override.** All THREE call `Path(override).expanduser()`, which raises `RuntimeError: Could not determine home directory` when the override is spelled `~someone` and no such account exists: `tools/hooks/common.py::operator_tokens_root`, `workflow_homes_root`, and `tools/run_workflow.py::_start_claims_root`. **The third is CLOSED at its caller and left open at the resolver** — `_exclusive_claim` catches `(OSError, RuntimeError, ValueError)` so the claim degrades to "proceed" as its contract says — so the candidate fix below applies to the FIRST TWO ONLY, and whoever picks this up must not read it as three sites. "Closed at its caller" means the driver no longer crashes; it does NOT mean the operator is told, so a mistyped claim root costs the cold-start serialisation with no signal. FAIL-CLOSED and operator-facing: `tools/hooks/cli.py` turns a raising hook into `_decision_error` and BLOCKS, so **a leaf gains nothing** and this is a wart rather than a hole — every Bash call in that shell is refused with `hook entrypoint failure: Could not determine home directory` and no mention of which variable caused it. Not fixed because the fix is a DECISION, not a `try`: making the resolver total means saying what an unresolvable override should resolve TO, and every answer either guards a path nobody writes to or silently guards a different tree. Candidate shape: catch in `tools/hooks/cli.py`'s entrypoint and name the offending `ATMOFAB_*` variable in the refusal, leaving the resolvers alone. Evidence: measured 2026-09-02 at PR #140's HEAD, and the same at `e0bae3d` for `workflow_homes_root` — the branch adds a second copy of the shape, not the shape. Record: https://github.com/seiya/atmofab/pull/140#issuecomment-5560401740 .
