spec_id: dynamics_advdiff_profile_1d_upwind_center2_euler1
spec_kind: profile
dependencies:
  components:
    - component_id: dynamics_advdiff_flux_1d_upwind_center2
      version_constraint: ">=0.2.0 <1.0.0"
    - component_id: dynamics_advection_diffusion_boundary_1d_periodic_copy
      version_constraint: ">=0.2.0 <1.0.0"
    - component_id: dynamics_advection_diffusion_time_update_1d_euler1
      version_constraint: ">=0.2.0 <1.0.0"
  profiles: []
  # No `infrastructure` entry, and none is allowed (issue #175): a profile is a
  # compile-time component-selection policy the host resolves at Compile, not a
  # certified code node. It builds nothing, so it has no runner harness to build
  # against — the node that ADOPTS this profile declares the harness.
